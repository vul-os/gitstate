{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core IPC only. All data flows over HTTP to the in-process daemon, not across IPC.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
